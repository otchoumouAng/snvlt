<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Reçu de Paiement</title>
    <style>
        body {
            font-family: 'Garamond', serif;
        }
        .container {
            width: 80%;
            margin: auto;
            border: 2px solid #000;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .content {
            margin-top: 20px;
        }
        .content p {
            margin: 5px 0;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 0.8em;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Reçu de paiement</h1>
        </div>
        <div class="content">
            <p><strong>Identifiant de la transaction :</strong> {{ transaction.tresorpayReceiptReference }}</p>
            <p><strong>Date :</strong> {{transaction.paidAt()|date("d/m/Y H:i:s") }}</p>
            <hr>
            <p><strong>Nom du client :</strong> {{ transaction.clientNom }} {{ transaction.clientPrenom }}</p>
            <p><strong>Téléphone :</strong> {{ transaction.telephone }}</p>
            <hr>
            <p><strong>Service :</strong> {{ transaction.service.designation }}</p>
            {% if transaction.attribution is not null %}
                <p><strong>PEF Concerné :</strong> {{ transaction.attribution.codeForet.denomination }} (N°{{ transaction.attribution.numeroDecision }})</p>
            {% endif %}
            <p><strong>Montant :</strong> {{ transaction.paidAmount|number_format(0, ',', ' ') }} FCFA</p>
        </div>
        <div class="footer">
            <p>Merci d'avoir utilisé nos services.</p>
        </div>
    </div>
</body>
</html>
