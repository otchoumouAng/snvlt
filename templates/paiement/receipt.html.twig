<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Reçu de Paiement</title>
    <style>
        body {
            font-family: 'Garamond', serif;
        }
        .container {
            width: 80%;
            margin: auto;
            border: 2px solid #000;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .content {
            margin-top: 20px;
        }
        .content p {
            margin: 5px 0;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 0.8em;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Avis de Recette</h1>
        </div>
        <div class="content">
            <p><strong>Identifiant de la transaction :</strong> {{ transaction.identifiant }}</p>
            <p><strong>Date :</strong> {{ "now"|date("d/m/Y H:i:s") }}</p>
            <hr>
            <p><strong>Nom du client :</strong> {{ transaction.clientNom }} {{ transaction.clientPrenom }}</p>
            <p><strong>Téléphone :</strong> {{ transaction.telephone }}</p>
            <hr>
            <p><strong>Service :</strong> {{ transaction.service.designation }}</p>
            <p><strong>Montant :</strong> {{ transaction.montantFcfa|number_format(0, ',', ' ') }} FCFA</p>
        </div>
        <div class="footer">
            <p>Merci d'avoir utilisé nos services.</p>
        </div>
    </div>
</body>
</html>
